type DataType = {
    active_id: number, 
    bid: number, 
    at: number, 
    ask: number, 
    max: number, 
    from: number, 
    phase: string, 
    close: number, 
    open: number, 
    min: number, 
    to: number, 
    size: number, 
    volume: number,
}   

export default DataType